1
sudo apt-get install git 
到GitHub上创建GitHub帐号 
 ssh-keygen -t rsa -C “用户名”,一直enter,enter,enter

Add SSH Key,title随便填，粘贴key。key就是id_rsa.pub内容。 
“ssh -T git@github.com

git config --global user.name "qyhboy"
git config --global user.email "1498457145@qq.com"


2
git init
git remote add origin git@github.com:qyhboy/file.git

git add newom or git add .

git commit -m 'this is for test1'
git push -f origin master 

gedit /etc/hosts 添加一行：192.30.252.128　　github.com
3
git clone git@github.com:qyhboy/test1.git
